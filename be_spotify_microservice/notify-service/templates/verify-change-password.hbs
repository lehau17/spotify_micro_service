<!DOCTYPE html>
<html>

    <head>
        <meta charset="UTF-8">
        <title>Verify Change Password</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                line-height: 1.6;
                margin: 0;
                padding: 0;
            }

            .container {
                max-width: 600px;
                margin: 20px auto;
                padding: 20px;
                border: 1px solid #ddd;
                border-radius: 8px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }

            .btn {
                display: inline-block;
                padding: 10px 20px;
                font-size: 16px;
                font-weight: bold;
                color: #ffffff;
                background-color: #1db954;
                border: none;
                border-radius: 5px;
                text-decoration: none;
            }

            .btn:hover {
                background-color: #14833b;
            }

            .footer {
                margin-top: 20px;
                font-size: 12px;
                color: #999;
                text-align: center;
            }
        </style>
    </head>

    <body>
        <div class="container">
            <h2>Hello {{customerName}},</h2>
            <p>We received a request to change your password. To verify your request, please click the button below:</p>
            <p>
                <a href="{{verificationUrl}}" class="btn">Verify Change Password</a>
            </p>
            <p>If you did not request this change, please ignore this email.</p>
            <p class="footer">© {{year}} Spotify. All rights reserved.</p>
        </div>
    </body>

</html>